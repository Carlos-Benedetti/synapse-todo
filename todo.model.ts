class Todo{
    id: number;
    checked: boolean;
    text: string;

    constructor(
        id:number,
        checked:boolean,
        text:string
    ){
        this.id = id
        this.checked = checked
        this.text = text
    }
}